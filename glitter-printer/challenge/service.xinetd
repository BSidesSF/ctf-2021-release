service service
{
    protocol       = tcp
    disable        = no
    port           = 515
    flags          = REUSE
    socket_type    = stream
    wait           = no
    user           = printer
    server         = /src/harness
    type           = UNLISTED
}
